import React from "react";
import styled from "styled-components";

const Help = ({ className }) => {
  return <div className={className}>How to Play</div>;
};

export default styled(Help)`
  background: var(--secondaryDark);
`;
